{
  "name": "simple-gpio",
  "version": "0.0.0",
  "main": "index.js",
  "scripts": {
    "test": "true",
    "build": "node-gyp rebuild",
    "clean": "node-gyp clean"
  },
  "gypfile": true,
  "dependencies": {
    "bindings": "^1.2.1",
    "node-addon-api": "^1.6.2"
  },
  "devDependencies": {
    "node-gyp": "^3.8.0"
  }
}
